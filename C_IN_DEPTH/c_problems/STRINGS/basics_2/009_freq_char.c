#include<stdio.h>
#include<ctype.h>


void count(char * str);
int main()
{
  char str[50];

  printf("enter a string\n");
  gets(str);
  count(str);

return 0;
}

void count(char * str)
{
  char str[50],ch;


}
